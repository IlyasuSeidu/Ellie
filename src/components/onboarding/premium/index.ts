/**
 * Premium Onboarding Components
 *
 * Export the active shared component used by the voice-first setup screens.
 */

export {
  PremiumButton,
  type PremiumButtonProps,
  type ButtonVariant,
  type ButtonSize,
  type IconPosition,
} from './PremiumButton';
